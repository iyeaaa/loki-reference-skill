name: SendGrinda CD Pipeline

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy SendGrinda to AWS EC2
    runs-on: ubuntu-latest

    steps:
    - name: Checkout SendGrinda Repository
      uses: actions/checkout@v4

    - name: Configure AWS EC2 SSH Connection
      uses: webfactory/ssh-agent@v0.9.0
      with:
        ssh-private-key: ${{ secrets.EC2_SSH_KEY }}

    - name: Register EC2 Host to Known Hosts
      run: |
        mkdir -p ~/.ssh
        ssh-keyscan -H ${{ secrets.EC2_HOST }} >> ~/.ssh/known_hosts

    - name: Prepare SendGrinda Environment Files
      run: |
        echo "${{ secrets.ADMIN_ENV }}" > ./admin/.env
        echo "${{ secrets.ELYSIA_SERVER_ENV }}" > ./elysia-server/.env

    - name: Deploy SendGrinda to EC2 via Rsync
      run: |
        echo "📦 Creating deployment directory on EC2..."
        ssh ${{ secrets.EC2_USERNAME }}@${{ secrets.EC2_HOST }} "mkdir -p ~/send-grid-test"

        echo "🚀 Syncing SendGrinda files to EC2..."
        rsync -avz --delete \
          --exclude='.git/' \
          --exclude='node_modules/' \
          --exclude='dist/' \
          --exclude='.next/' \
          --exclude='.turbo/' \
          --exclude='*.log' \
          --exclude='.DS_Store' \
          ./ ${{ secrets.EC2_USERNAME }}@${{ secrets.EC2_HOST }}:~/send-grid-test/

    - name: Build and Deploy SendGrinda with Docker Compose
      run: |
        ssh ${{ secrets.EC2_USERNAME }}@${{ secrets.EC2_HOST }} << 'EOF'
          set -e
          set -o pipefail

          cd ~/send-grid-test

          echo "💾 Backing up current containers for rollback..."
          docker compose ps -q > /tmp/previous_containers.txt

          echo "🔨 Building SendGrinda Docker images..."
          docker compose build --pull

          echo "🚢 Deploying SendGrinda containers..."
          docker compose up -d --no-build --remove-orphans

          echo "🏥 Running health checks (timeout: 60s)..."
          HEALTH_CHECK_TIMEOUT=60
          HEALTH_CHECK_INTERVAL=5
          ELAPSED=0

          while [ $ELAPSED -lt $HEALTH_CHECK_TIMEOUT ]; do
            ADMIN_STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/health || echo "000")
            ELYSIA_STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:3001/health || echo "000")

            if [ "$ADMIN_STATUS" = "200" ] && [ "$ELYSIA_STATUS" = "200" ]; then
              echo "✅ SendGrinda services are healthy and ready!"
              break
            fi

            echo "⏳ Health check in progress... (Admin: $ADMIN_STATUS, Elysia: $ELYSIA_STATUS) [$ELAPSED/$HEALTH_CHECK_TIMEOUT s]"
            sleep $HEALTH_CHECK_INTERVAL
            ELAPSED=$((ELAPSED + HEALTH_CHECK_INTERVAL))
          done

          if [ $ELAPSED -ge $HEALTH_CHECK_TIMEOUT ]; then
            echo "❌ SendGrinda health check failed - Rolling back deployment..."
            docker compose down
            if [ -f /tmp/previous_containers.txt ]; then
              echo "🔄 Restoring previous containers..."
              docker compose up -d --no-build
            fi
            exit 1
          fi

          echo "🧹 Cleaning up unused Docker images..."
          docker image prune -f

          echo -e "\n📦 SendGrinda Container Status:"
          docker compose ps

          echo -e "\n📋 SendGrinda Service Logs (last 10 lines):"
          docker compose logs --tail=10
        EOF

    - name: Verify SendGrinda Deployment
      run: |
        echo "🔍 Verifying SendGrinda deployment from external network..."
        sleep 5

        ADMIN_CHECK=$(curl -s -o /dev/null -w "%{http_code}" http://${{ secrets.EC2_HOST }}:3000/health || echo "000")
        ELYSIA_CHECK=$(curl -s -o /dev/null -w "%{http_code}" http://${{ secrets.EC2_HOST }}:3001/health || echo "000")

        if [ "$ADMIN_CHECK" != "200" ] || [ "$ELYSIA_CHECK" != "200" ]; then
          echo "❌ SendGrinda deployment verification failed!"
          echo "   Admin service: $ADMIN_CHECK (expected: 200)"
          echo "   Elysia service: $ELYSIA_CHECK (expected: 200)"
          exit 1
        fi

        echo "✅ SendGrinda deployment verified successfully!"
        echo "   Admin service: $ADMIN_CHECK ✓"
        echo "   Elysia service: $ELYSIA_CHECK ✓"
        echo ""
        echo "🎉 SendGrinda is now live on AWS EC2!"