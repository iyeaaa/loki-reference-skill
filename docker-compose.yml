services:
  sendgrid-webhook:
    build: .
    container_name: sendgrid-webhook
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
      - DEBUG=false
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped